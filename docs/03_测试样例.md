# 03 测试样例

包含两类：
1. **手动验收场景**（最终交付前逐项验证）
2. **自动化测试样例**（Unity Test Framework：EditMode 逻辑测试 + PlayMode 集成测试）

测试目录约定：`Assets/Game/Tests/`（EditMode + PlayMode，使用程序集定义引用 `Game.Runtime` 与 `XNode`）。

---

## 一、手动验收场景（对应设计文档「最终验收场景」）

> 运行 Demo，逐项检查并勾选。

### A. 敌人 AI 状态闭环

| # | 场景操作 | 预期结果 | 通过 |
|---|---|---|---|
| A1 | 观察敌人空闲 | 敌人 Idle 停留随机时间后进入 Patrol | ☐ |
| A2 | 敌人多个巡逻点 | 敌人在多个巡逻点间依次行动 | ☐ |
| A3 | 从敌人背后经过 | 不被立即发现（背后不在视野角内） | ☐ |
| A4 | 进入视野并持续暴露 | 警觉值上升，达到阈值后敌人开始追击（Chase） | ☐ |
| A5 | 中间有墙体遮挡 | 障碍物阻挡视线，不触发发现 | ☐ |
| A6 | 进入武器射程 | 敌人停止追击并进入 Attack，开始攻击 | ☐ |
| A7 | 躲到墙后丢失视野 | 敌人前往最后发现位置搜索（Investigate） | ☐ |
| A8 | 搜索失败 | 敌人返回巡逻区域（Return → Patrol） | ☐ |
| A9 | 搜索期间再次暴露 | 敌人立即重新追击（Chase） | ☐ |
| A10 | 击杀敌人 | 敌人进入 Dead，停止行为并播放死亡效果 | ☐ |

### B. 战斗系统（玩家/敌人共用）

| # | 场景操作 | 预期结果 | 通过 |
|---|---|---|---|
| B1 | 玩家持枪射击敌人 | 弹丸命中敌人扣血，命中障碍销毁 | ☐ |
| B2 | 玩家持近战武器攻击 | Active 阶段命中，一次攻击同一目标只扣一次血 | ☐ |
| B3 | 敌人持剑追击玩家 | 追到约 1.5m 后挥砍；玩家拉开距离后继续追击 | ☐ |
| B4 | 敌人持枪射击 | 在约 8~9m 距离停止并射击 | ☐ |
| B5 | 攻击前摇 | 敌人射击有明显前摇/瞄准线，给玩家反应时间 | ☐ |
| B6 | 阵营判定 | 敌人子弹不伤害敌人，玩家子弹不伤害玩家 | ☐ |
| B7 | 玩家死亡 | 禁止继续控制，显示结果界面 | ☐ |
| B8 | 敌人全灭 | 显示胜利结果界面 | ☐ |
| B9 | 按 R 重开 | 场景重置，可重新游玩 | ☐ |

### C. 技能系统（Slate）

| # | 场景操作 | 预期结果 | 通过 |
|---|---|---|---|
| C1 | 玩家释放挥剑技能 | 按时间轴播放：面向目标→前摇→伤害窗口→后摇 | ☐ |
| C2 | 技能冷却 | 冷却期间无法再次施法，冷却结束后可施法 | ☐ |
| C3 | 技能范围 | 超出最大施法距离时无法施法 | ☐ |
| C4 | 施法中锁定移动 | lockMovement=true 时施法期间无法移动，结束后恢复 | ☐ |
| C5 | 技能被中断 | 死亡/打断时技能停止，移动锁定解除 | ☐ |
| C6 | 冲刺斩技能 | Dash 位移 + 伤害窗口时序正确 | ☐ |
| C7 | 编辑器时间轴预览 | 预览播放动画/特效但不扣血、不生成永久弹丸 | ☐ |

### D. 调试与性能

| # | 场景操作 | 预期结果 | 通过 |
|---|---|---|---|
| D1 | 按 F1 | AI 调试信息（状态/视野/目标线）显示/隐藏 | ☐ |
| D2 | 3~5 个敌人同场景 | 每个敌人独立运行，互不干扰（共用同一张 xNode 图） | ☐ |
| D3 | Inspector 调参 | 修改 EnemyConfig/WeaponConfig 后敌人表现立即变化 | ☐ |

---

## 二、自动化测试样例

> **实现状态（2026-08-22）**：EditMode 测试已实现 10 项并全部通过（T1 伤害/T2 阵营/T3 攻击范围/T5 配置钳制/T6 图校验/T9/T10 命中逻辑）。PlayMode 集成测试（T7/T8/T11）代码已就绪，需在本地 Unity Test Runner 运行。

### EditMode 测试（纯逻辑，无需场景）

#### T1. 伤害系统

```csharp
[Test]
public void Health_TakeDamage_ReducesHealth()
{
    var go = new GameObject("Health");
    var health = go.AddComponent<Health>();
    health.Initialize(100f);

    health.TakeDamage(new DamageInfo(30f, null, null, Vector3.zero));

    Assert.AreEqual(70f, health.CurrentHealth);
    Assert.IsTrue(health.IsAlive);
}

[Test]
public void Health_Death_FiresDiedEvent_Once()
{
    var go = new GameObject("Health");
    var health = go.AddComponent<Health>();
    health.Initialize(10f);
    int diedCount = 0;
    health.Died += _ => diedCount++;

    health.TakeDamage(new DamageInfo(10f, null, null, Vector3.zero));
    health.TakeDamage(new DamageInfo(5f, null, null, Vector3.zero));

    Assert.IsFalse(health.IsAlive);
    Assert.AreEqual(0f, health.CurrentHealth);
    Assert.AreEqual(1, diedCount); // 死亡后不再重复触发
}
```

#### T2. 阵营系统

```csharp
[Test]
public void FactionMember_Enemy_CanDamage_OnlyPlayer()
{
    var enemyFaction = new FactionMember { Faction = Faction.Enemy };
    var playerFaction = new FactionMember { Faction = Faction.Player };
    var neutralFaction = new FactionMember { Faction = Faction.Neutral };

    Assert.IsTrue(enemyFaction.CanDamage(playerFaction));
    Assert.IsFalse(enemyFaction.CanDamage(enemyFaction));
    Assert.IsFalse(enemyFaction.CanDamage(neutralFaction));
}
```

#### T3. 攻击范围判定（CharacterCombat）

```csharp
[Test]
public void Combat_IsTargetInAttackRange_Melee()
{
    // 装备 MeleeWeaponConfig(attackRange=1.8)
    // 目标距离 1.0 → true；距离 3.0 → false
    // 目标距离 1.9 + tolerance 0.4 → true（滞后区间）
}
```

#### T4. 近战武器去重

```csharp
[Test]
public void MeleeWeapon_SingleAttack_HitsTargetOnce()
{
    // 多个 collider 属于同一 IDamageable
    // 一次攻击 Active 阶段 → 该目标只收到一次 TakeDamage
}
```

#### T5. 敌人配置 OnValidate

```csharp
[Test]
public void EnemyConfig_OnValidate_ClampsValues()
{
    // maxHealth 设 0 → 被钳制为 1
    // chaseSpeed < patrolSpeed → 被钳制为 patrolSpeed
    // viewAngle 设 500 → 被钳制为 360
}
```

#### T6. xNode 图校验器

```csharp
[Test]
public void GraphValidator_EmptyGraph_ReportsError()
{
    // 无 Entry / 端口未连接 → 校验器返回错误列表非空
}

[Test]
public void GraphValidator_DefaultGraph_Passes()
{
    // DefaultEnemyStateGraph.asset → 校验通过
}
```

### PlayMode 测试（真实场景/物理）

#### T7. 敌人状态切换集成

```csharp
[UnityTest]
public IEnumerator Enemy_SeesPlayer_TransitionsToChase()
{
    // 布置：敌人(带 Perception/Motor/Runner + DefaultGraph) + 玩家在视野内
    // 等待若干帧/警觉值充满
    // Assert: runner.CurrentState is ChaseStateNode
    yield return null;
}
```

#### T8. 敌人丢失目标 → Investigate → Return

```csharp
[UnityTest]
public IEnumerator Enemy_LosesTarget_Investigates_ThenReturns()
{
    // 玩家进入视野触发 Chase → 玩家瞬移出视野
    // 等待：进入 Investigate → 搜索结束 → Return → Patrol
    // Assert 各阶段 CurrentState 类型
}
```

#### T9. 弹丸命中敌人扣血

```csharp
[UnityTest]
public IEnumerator Projectile_HitsEnemy_DealsDamage()
{
    // 生成 RangedWeapon + 敌人 Health(100)
    // 发射弹丸朝敌人 → 等待命中
    // Assert: enemyHealth.CurrentHealth < 100
}
```

#### T10. 技能时间轴伤害窗口

```csharp
[UnityTest]
public IEnumerator Skill_SwordSlash_DamageInActiveWindow()
{
    // 释放 Skill_SwordSlash
    // 0.15s 时（伤害窗口前）→ 目标未扣血
    // 0.30s 时（伤害窗口内）→ 目标扣血一次
}
```

#### T11. 多敌人共享状态图互不干扰

```csharp
[UnityTest]
public IEnumerator TwoEnemies_ShareGraph_IndependentState()
{
    // 两个敌人引用同一 EnemyStateGraph.asset
    // 一个进入 Chase，另一个保持 Patrol
    // Assert 两个 Runner 的 CurrentState 不同且各自黑板数据独立
}
```

---

## 三、测试优先级

| 优先级 | 测试 | 说明 |
|---|---|---|
| P0（必须） | T1 伤害系统、T2 阵营、T7 敌人状态切换、T8 丢失→搜索→返回、A1~A10 | 核心闭环正确性 |
| P1（重要） | T3 攻击范围、T4 去重、T9 弹丸、T11 多敌人共用图、B1~B9 | 系统完整性与稳定性 |
| P2（加分） | T5 OnValidate、T6 图校验器、T10 技能窗口、C1~C7、D1~D3 | 配置健壮性与技能表现 |

> 自动化测试目标：核心逻辑（伤害/阵营/范围/去重/状态切换）达到 100% 覆盖；编辑器校验器能提前拦截配置错误。

### 已实现测试文件

```text
Assets/Game/Tests/EditMode
├── CombatTests.cs            T1 伤害系统 + T2 阵营系统
├── WeaponTests.cs            T3 攻击范围判定
├── ConfigTests.cs            T5 敌人配置 OnValidate
├── GraphValidatorTests.cs    T6 xNode 图校验器
└── WeaponLogicTests.cs       T9/T10 命中逻辑（OverlapBox/阵营过滤）

Assets/Game/Tests/PlayMode
└── EnemyAiPlayModeTests.cs   T7/T8/T11 状态机集成（代码就绪）
```

运行方式：Unity Test Runner（Window > General > Test Runner）选择 EditMode / PlayMode 运行。
